{
  "name": "should-i-make-tea",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm install && npx vite build && git checkout gh-pages && cp -r dist/* . && rm -rf dist node_modules .gitignore README.md index.html main.js package.json package-lock.json vite.config.js && git add -A && git commit -m 'Deploy' && git push origin gh-pages && git checkout main"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}